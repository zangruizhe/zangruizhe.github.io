baseURL = 'https://zangruizhe.github.io/'
languageCode = 'en-us'
title = 'Puzzle Hunt with Racer'
theme = "mainroad"
paginate = "10" # Number of posts per page
disqusShortname = "" # DEPRECATED! Use .Services.Disqus.Shortname
googleAnalytics = "" # DEPRECATED! Use .Services.googleAnalytics.ID

[services]
[services.disqus]
shortname = "racer_zang" # Enable Disqus by entering your Disqus shortname

[services.googleAnalytics]
ID = "" # Enable Google Analytics by entering your tracking ID

[Author]
  name = "Zang Ruizhe(Racer)"
  bio = "F#, XGBoost, C++, HFT, Bayesian inference, PyMC, AoC"
  avatar = "img/avatar.png"

[Params]
  description = "Why not, give it a shot!" # Description of your site
  opengraph = true
  twitter_cards = false
  readmore = true # Show "Read more" button in list if true
  authorbox = false
  toc = true # Enable Table of Contents
  pager = true # Show pager navigation (prev/next links) at the bottom of pages if true
  post_meta = ["date", "categories"] # Order of post meta information
  mainSections = ["post", "docs"]
  comments = true

[Params.logo]
  subtitle = "Why not, give it a shot!" # Logo subtitle

[Params.sidebar]
  home = "right"   # Configure layout for home page
  list = "right"   # Configure layout for list pages
  single = false # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["search", "recent", "categories", "taglist", "social"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  categories_counter = true # Enable counter for each category in "Categories" widget
  tags_counter = true # Enable counter for each tag in "Tags" widget

[Params.widgets.social]
cached = false # activate cache if true
# Enable parts of social widget
twitter = "Racer_zang"
github = "zangruizhe"
email = "ruizhe DOT zang AT gmail DOT com "